use crate::update::{project_root, Mode};

// pub fn generate(mode: Mode) -> anyhow::Result<()> {
//     let mut fs = ts_bindgen::StdFs;

//     ts_bindgen::generate_rust_string_for_typescript(fs, module)
// }
