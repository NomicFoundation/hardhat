// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

// Imported by the test sources through a non-relative path, so the test run
// exercises the import mappings passed to EDR.
contract BaseTest {

}
