// A minimal script you can run with `npx hardhat run scripts/example.ts`.
// In native mode it executes via Node.js' built-in type stripping, with no
// transpiler involved.

const message: string = "Hello from a native TypeScript Hardhat script!";

console.log(message);
