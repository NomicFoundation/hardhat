const assert = require("assert");

describe("test suite 1", function () {
  it("should fail", async function () {
    assert(false);
  });

  it("should also fail", async function () {
    assert(false);
  });
});
