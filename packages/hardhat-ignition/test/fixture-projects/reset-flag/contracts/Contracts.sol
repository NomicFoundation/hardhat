// SPDX-License-Identifier: UNLICENSED
pragma solidity ^0.8.9;

contract Foo {
  function isFoo() public pure returns (bool) {
    return true;
  }
}

contract Bar {
  function isBar() public pure returns (bool) {
    return true;
  }
}

contract Baz {
  function isBaz() public pure returns (bool) {
    return true;
  }
}
