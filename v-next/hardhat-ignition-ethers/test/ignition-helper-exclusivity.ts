describe("ignition helper mutual exclusivity", () => {
  // TODO: HH3 Bring back with the other plugin check
  it.skip("should error when loaded in conjunction with hardhat-ignition-viem", async function () {
    // assert.throws(
    //   () => require("hardhat"),
    //   /Found viem and ethers, but only one Hardhat Ignition extension plugin can be used at a time\./,
    // );
  });
});
