import { describe, it } from "node:test";

describe("spawn", () => {
  it.todo("should execute the command and wait for it to finish");
  it.todo("should throw if the command exits with a non-zero exit code");
});
