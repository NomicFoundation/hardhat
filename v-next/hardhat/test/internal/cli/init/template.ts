import { describe, it } from "node:test";

describe("getTemplates", () => {
  it.todo("should return a non-empty array of templates");
  it.todo("should ensure the templates have unique names");
  it.todo("should ensure the template files exist");
});
