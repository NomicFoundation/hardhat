---
"hardhat": minor
"@nomicfoundation/hardhat-ethers": minor
"@nomicfoundation/hardhat-keystore": minor
"@nomicfoundation/hardhat-utils": minor
---

Update the cryptography dependencies to their latest major versions: `@noble/hashes` and `@noble/ciphers` to 2, `ethereum-cryptography` to 3, and `micro-eth-signer` to 0.19.
