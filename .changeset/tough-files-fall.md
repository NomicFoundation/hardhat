---
"@nomicfoundation/hardhat-node-test-reporter": minor
---

Add a special case to the error formatting when a `SolidityError` is found, so that Solidity stack traces are more prominent. Thanks, @gultekinmakif!
