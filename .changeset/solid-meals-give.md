---
"@nomicfoundation/hardhat-mocha": patch
"@nomicfoundation/hardhat-node-test-runner": patch
---

Add `--grep` tests for mocha, and add a (currently skipped) `--grep` fixture/test for nodejs pending a Node grep fix.
